<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blockchain Explorer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>🔗 Blockchain Explore</h1>

        <!-- Форма поиска по хешу -->
        <form method="POST" class="mt-5">
            <div class="form-group">
                <label for="input">Поиск блока</label>
                <input type="text"
                       value="<%= obj.hash %>"
                       name="input"
                       class="form-control"
                       id="input"
                       placeholder="Введите хеш блока">
            </div>
            <button type="submit" class="btn-primary">Найти</button>
        </form>

        <!-- Форма поиска по адресу -->
        <form method="POST" class="mt-5">
            <div class="form-group">
                <label for="address">Поиск по адресу</label>
                <input type="text"
                       value="address"
                       name="address"
                       class="form-control"
                       id="address"
                       placeholder="Введите адрес кошелька">
            </div>
            <button type="submit" class="btn-primary">Найти</button>
        </form>

        <!-- Ссылка на последний блок -->
        <div id="last"><a href="/last">Последний блок</a></div>

        <!-- Пример хеша -->
        <p>0xe458cebd99c2b3e45229e116b7647af5ba8252803b6bff298f162628a4a08a78</p>
    </div>
</body>
</html>